module.exports = {
    darkMode: 'class', // svarīgi
    theme: {
        extend: {
            maxWidth: {
                'nav-width': 'calc(100vw - 296px)',
            },
        }
    },
    plugins: [],
}